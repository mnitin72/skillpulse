version: '3.9'

services:

  backend:
    build: ./backend
    container_name: skillpulse-backend

    ports:
      - "8080:8080"

  frontend:
    build: ./frontend
    container_name: skillpulse-frontend

    ports:
      - "80:80"

    depends_on:
      - backend
